name: Secret Hygiene

on:
  push:
    branches:
      - main
      - test
  pull_request:

jobs:
  scan:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Secret patterns check
        run: |
          set -e
          # Scan only tracked env config files (not docs) to avoid placeholder noise.
          echo "checking .env files only"
          for FIELD in SUPER_ADMIN_PASSWORD JWT_SECRET REFRESH_SECRET TWO_FACTOR_ENCRYPTION_KEY GOOGLE_CLIENT_SECRET; do
            git grep -n -E "${FIELD}=.+$" -- '*.env' '*.env.example' 2>/dev/null | grep -v 'replace-with' | grep -v 'your-google-client-secret' && {
              echo "ERROR: ${FIELD} hardcoded value found in tracked config files";
              exit 1;
            }
          done

      - name: Run detect-secrets
        run: |
          python -m pip install --upgrade pip
          python -m pip install detect-secrets
          if [ -f .secrets-baseline ]; then
            detect-secrets scan --baseline .secrets-baseline
          else
            echo "No baseline found; running detect-secrets without baseline."
            detect-secrets scan
          fi
